{
    "name": "dump-app",
    "scripts": {
        "build": "npm run build --workspace=website && npm run generate",
        "generate": " npx prisma migrate deploy && npx prisma generate",
        "test": "playwright test"
    },
    "workspaces": [
        "website"
    ]
}